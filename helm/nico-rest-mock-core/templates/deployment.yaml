apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nico-rest-mock-core.fullname" . }}
  labels:
    {{- include "nico-rest-mock-core.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "nico-rest-mock-core.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nico-rest-mock-core.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "nico-rest-mock-core.serviceAccountName" . }}
      securityContext:
        {{- with .Values.podSecurityContext }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        # Required for virt-customize user-data injection (always on when provisioning).
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      containers:
        - name: {{ include "nico-rest-mock-core.name" . }}
          securityContext:
            {{- with .Values.securityContext }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            # Required for virt-customize user-data injection (always on when provisioning).
            runAsUser: 0
            runAsGroup: 0
            privileged: true
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: false
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" | quote }}
          args:
            - --config=/config/machines.yaml
            - --listen=:{{ .Values.service.port }}
            - --log-level={{ .Values.logLevel }}
            {{- if .Values.libvirt.enabled }}
            - --libvirt-endpoint={{ .Values.libvirt.endpoint }}
            - --libvirt-storage-pool={{ .Values.libvirt.storagePool }}
            - --libvirt-volume-gib={{ include "nico-rest-mock-core.libvirtVolumeGiB" . }}
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - --state-file={{ .Values.persistence.stateFile }}
            - --image-cache-dir={{ .Values.persistence.imageCacheDir }}
            - --temp-dir={{ .Values.persistence.tempDir }}
            {{- end }}
          ports:
            - name: grpc
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: state
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
            - name: dev
              mountPath: /dev
            - name: var-lock
              mountPath: /var/lock
            - name: run
              mountPath: /run
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          env:
            # Always set for virt-customize; no chart value required to enable injection.
            - name: LIBGUESTFS_BACKEND
              value: direct
            - name: LIBGUESTFS_SKIP_OS_CHECK
              value: "1"
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "nico-rest-mock-core.fullname" . }}-config
        {{- if .Values.persistence.enabled }}
        - name: state
          persistentVolumeClaim:
            claimName: {{ include "nico-rest-mock-core.fullname" . }}-state
        {{- end }}
        - name: dev
          hostPath:
            path: /dev
            type: Directory
        - name: var-lock
          emptyDir: {}
        - name: run
          emptyDir: {}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
