apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nico-rest-mock-core.fullname" . }}
  labels:
    {{- include "nico-rest-mock-core.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "nico-rest-mock-core.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nico-rest-mock-core.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "nico-rest-mock-core.serviceAccountName" . }}
      {{- if .Values.libvirt.enabled }}
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      {{- else }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "nico-rest-mock-core.name" . }}
          {{- if .Values.libvirt.enabled }}
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            privileged: {{ .Values.libvirt.privileged }}
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: false
          {{- else }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" | quote }}
          args:
            - --config=/config/machines.yaml
            - --listen=:{{ .Values.service.port }}
            - --log-level={{ .Values.logLevel }}
            {{- if .Values.libvirt.enabled }}
            - --libvirt-endpoint={{ .Values.libvirt.endpoint }}
            - --libvirt-storage-pool={{ .Values.libvirt.storagePool }}
            - --libvirt-volume-gib={{ include "nico-rest-mock-core.libvirtVolumeGiB" . }}
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - --state-file={{ .Values.persistence.stateFile }}
            - --image-cache-dir={{ .Values.persistence.imageCacheDir }}
            - --temp-dir={{ .Values.persistence.tempDir }}
            {{- end }}
          ports:
            - name: grpc
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: state
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
            {{- if and .Values.libvirt.enabled .Values.libvirt.mountKVM (not .Values.libvirt.mountHostDev) }}
            - name: kvm
              mountPath: /dev/kvm
            {{- end }}
            {{- if and .Values.libvirt.enabled .Values.libvirt.mountHostDev }}
            - name: dev
              mountPath: /dev
            {{- end }}
            {{- if and .Values.libvirt.enabled .Values.libvirt.mountLibModules }}
            - name: lib-modules
              mountPath: /lib/modules
              readOnly: true
            {{- end }}
            {{- if .Values.libvirt.enabled }}
            - name: var-lock
              mountPath: /var/lock
            - name: run
              mountPath: /run
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          env:
            - name: LIBGUESTFS_BACKEND
              value: direct
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "nico-rest-mock-core.fullname" . }}-config
        {{- if .Values.persistence.enabled }}
        - name: state
          persistentVolumeClaim:
            claimName: {{ include "nico-rest-mock-core.fullname" . }}-state
        {{- end }}
        {{- if and .Values.libvirt.enabled .Values.libvirt.mountKVM (not .Values.libvirt.mountHostDev) }}
        - name: kvm
          hostPath:
            path: /dev/kvm
            type: CharDevice
        {{- end }}
        {{- if and .Values.libvirt.enabled .Values.libvirt.mountHostDev }}
        - name: dev
          hostPath:
            path: /dev
            type: Directory
        {{- end }}
        {{- if and .Values.libvirt.enabled .Values.libvirt.mountLibModules }}
        - name: lib-modules
          hostPath:
            path: /lib/modules
            type: Directory
        {{- end }}
        {{- if .Values.libvirt.enabled }}
        - name: var-lock
          emptyDir: {}
        - name: run
          emptyDir: {}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
